export const metadata = {
  title: '建立管理員帳號',
  description: '建立系統管理員帳號',
  viewport: {
    width: 'device-width',
    initialScale: 1,
  },
}

export default function CreateAdminLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
